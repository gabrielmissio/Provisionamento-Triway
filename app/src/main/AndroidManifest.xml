<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmissio.provisionamentotriway">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".eg8120l5.OpcoesProvisionamento8120l5"></activity>
        <activity android:name=".eg8120l5.Provisionamento8120l5" />
        <activity android:name=".provisionamentosemwifi.OpcoesProvicionamentoSemWifi" />
        <activity android:name=".provisionamentosemwifi.ProvisionamentoSemWifi" />
        <activity android:name=".provisionamentocomwifi.SsidPassword" />
        <activity android:name=".provisionamentocomwifi.Provisionamento" />
        <activity android:name=".provisionamentocomwifi.OpcoesProvicionamento" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>